#include "Fetch.hpp"

template <typename T>
T app_helpers::fetch_helper::Fetch<T>::Get(const std::string &url, const std::map<std::string, std::string> &queries,
                                           const std::string &endpoint)
{
    T t;
    return t;
}
